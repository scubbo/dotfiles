export VAULT_ADDR=https://vault.legalzoom.com
export BOUNDARY_ADDR=https://boundary.devops.prd.aws-01.legalzoom.com

# https://stackoverflow.com/a/51939526
export PATH="/usr/local/opt/python/libexec/bin:$PATH"
# My own scripts!
export PATH="/Users/jackjackson/Code/scubbo-scripts:$PATH"

# Generated with `/opt/homebrew/bin/brew shellenv`
export HOMEBREW_PREFIX="/opt/homebrew";
export HOMEBREW_CELLAR="/opt/homebrew/Cellar";
export HOMEBREW_REPOSITORY="/opt/homebrew";
export PATH="/opt/homebrew/bin:/opt/homebrew/sbin${PATH+:$PATH}";
export MANPATH="/opt/homebrew/share/man${MANPATH+:$MANPATH}:";
export INFOPATH="/opt/homebrew/share/info:${INFOPATH:-}";
# https://stackoverflow.com/a/43087047
bindkey -e

alias ttt="turbo --affected typecheck test"
alias plf="git fetch origin && git merge origin main; pnpm install && git add -A && git commit -m 'pnpm-lock conflict fix' && git push"

# NVM setup
export NVM_DIR="$HOME/.nvm"
[ -s "/opt/homebrew/opt/nvm/nvm.sh" ] && \. "/opt/homebrew/opt/nvm/nvm.sh"  # This loads nvm
[ -s "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm" ] && \. "/opt/homebrew/opt/nvm/etc/bash_completion.d/nvm"  # This loads nvm bash_completion

# pnpm
export PNPM_HOME="/Users/jackjackson/Library/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end


ENV_ZSHRC="${HOME}/.env/zshrc"
if [[ -f "$ENV_ZSHRC" ]]; then
  source "$ENV_ZSHRC"
fi
